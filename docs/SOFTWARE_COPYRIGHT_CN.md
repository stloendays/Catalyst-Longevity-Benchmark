# 催化剂寿命数据分析与实验辅助系统

> 软件著作权材料准备稿。工程内部英文名可保留 `Catalyst Longevity Research`，对外登记与界面建议统一使用中文全称。

## 一、软件定位

本系统面向高校实验室、科研院所、催化剂研发团队及化工企业研发人员，用于整理催化剂长期稳定性实验数据，检查数据质量和实验条件，计算寿命指标，进行受条件约束的同时间对比，并根据当前观测结果给出后续实验建议。系统同时支持论文资料整理、催化剂与时间点关联、本地项目保存和 PDF 报告输出。

系统采用 Windows 原生桌面程序方式运行，实验数据、项目资料和人工确认状态均可保存在本地项目文件中，适合科研项目连续记录、阶段分析和结果复查。

## 二、主要应用价值

### 1. 降低实验数据整理成本

支持直接导入 CSV 和 Excel 数据，自动完成催化剂分组、时间排序、性能保持率和寿命指标计算。数据导入后自动检查重复时间点、异常数值、实验条件缺失和同一催化剂内部条件变化，并给出明确处理建议。

### 2. 规范不同催化剂之间的横向比较

系统在比较不同催化剂前自动检查温度、空速、压力和进料条件。当实验条件明确不一致时，停止输出直接领先结论，避免将不同测试条件下的数据简单排名。

### 3. 将寿命结果直接转化为实验安排

T95、T90、T80 保留与实际观测一致的区间表达。当 T90 尚未达到时，系统可根据当前测试长度和采样间隔建议延长测试；当 T90 位于两个采样点之间时，可建议在区间内增加采样点；当首次观测已经低于 T90 时，可建议提前首个采样时间。

### 4. 提供可复查的同时间对比

系统自动寻找两个催化剂最近的共同实际观测时间，显示双方性能值、差值和当前较高者。若实验条件不一致，则只显示检查结果，不给出直接领先判断；实验条件填写不完整时，比较结果标记为仅供参考。

### 5. 将论文资料与实验对象建立可追溯关联

系统可读取带文本层的 PDF 以及 TXT、Markdown、CSV、TSV 等资料，提取 DOI、温度、测试时长、转化率等候选信息，并允许用户将资料条目关联到具体催化剂和时间点。只有经过人工确认的资料才进入 AI 可用资料区。

### 6. 支持连续科研项目管理

实验记录、资料条目、关联关系和确认状态可统一保存到 `.clrproj` 本地 SQLite 项目文件中。后续实验完成后可重新打开原项目继续分析，无需重复整理历史数据。

### 7. 形成可归档的分析报告

系统可将催化剂寿命结果、数据检查、条件检查、同时间比较、实验建议和资料来源整理为 PDF 报告，用于组会讨论、实验记录归档、项目阶段总结和内部技术交流。

## 三、内置数据集

系统自带 5 组原生内置数据，不依赖外部文件即可演示核心功能。所有内置数据均明确标记为模拟数据，只用于软件功能演示、流程验证和教学，不作为真实实验结论。

1. **长期稳定性对比**：三种催化剂在一致条件下的长期性能数据，用于展示性能曲线、保持率、T90 和同时间对比。
2. **T90 区间定位**：用于展示 T90 位于两个实测时间点之间时的区间表达和加密采样建议。
3. **长周期寿命下限**：包含最长 1000 h 的稳定性数据，用于展示测试结束仍未达到 T90 时的寿命下限和延长实验建议。
4. **实验条件差异检查**：两组催化剂测试温度不同，用于展示系统自动发现条件差异并停止直接排名。
5. **数据质量检查**：包含重复时间点、条件缺失和同一样品条件变化，用于展示完整度评分和处理建议。

内置数据集可从首页或数据页直接选择，载入后自动进入与用户导入数据相同的分析流程。

## 四、功能模块

### 1. 项目

- 新建项目
- 打开项目
- 保存项目
- 另存为项目
- 本地 SQLite 项目持久化

### 2. 首页

- 催化剂数量和数据点概览
- 最长测试时间
- 条件检查状态
- 当前领先催化剂
- T90 状态
- 资料状态
- 长期性能曲线
- 曲线悬停查看数据
- 单击催化剂曲线进行聚焦
- 内置数据集快速载入

### 3. 数据

- CSV 导入
- Excel `.xlsx` 导入
- 内置数据集选择
- 原始数据表查看
- 自动数据检查
- 数据完整度评分
- 重复时间点检查
- 异常性能值检查
- 实验条件缺失检查
- 同一催化剂条件变化检查

### 4. 资料

- PDF 文本层读取
- TXT / Markdown / CSV / TSV 资料读取
- DOI、温度、测试时长等信息识别
- 资料与催化剂关联
- 资料与时间点关联
- 人工确认 / 取消确认
- AI 可用资料预览

### 5. 分析

#### 寿命指标

- 保持率
- T95
- T90
- T80
- 左删失、区间和右删失状态保留

#### 同时间对比

- 自动寻找两个催化剂最近共同时间
- 显示双方性能值
- 计算差值
- 条件不一致时停止直接判断

#### 实验建议

- 时间点不足提示
- T90 未达到时建议延长实验
- T90 位于区间时建议增加中间采样点
- 首次观测已低于 T90 时建议提前采样
- 实验条件缺失时提示补全记录

### 6. AI 助手

- 显示当前 AI 可用资料
- 未确认资料自动排除
- 为模型接口保留受控输入边界
- 分析结果和资料来源可追溯

### 7. 设置

- 软件信息和技术环境
- 本地运行说明
- 数据格式和项目格式说明

## 五、典型使用流程

1. 新建催化剂研究项目。
2. 选择一个内置数据集快速查看完整流程，或导入 CSV / Excel 实验数据。
3. 查看数据检查结果，处理重复记录和实验条件缺失。
4. 在首页查看长期性能曲线和总体状态。
5. 在分析页查看 T95 / T90 / T80。
6. 使用同时间对比比较不同催化剂。
7. 根据实验建议确定下一轮测试时长和采样点。
8. 在资料页导入论文，并关联到具体催化剂和时间。
9. 对资料完成确认后，可作为 AI 助手的输入资料。
10. 保存项目并导出 PDF 分析报告。

## 六、软件著作权材料截图建议

1. 软件首页及左侧导航栏。
2. 内置数据集选择窗口。
3. 数据导入界面。
4. 数据检查和完整度评分。
5. 长期性能曲线悬停状态。
6. 寿命指标页面。
7. 同时间对比页面。
8. 实验建议页面。
9. 资料整理和人工确认页面。
10. AI 可用资料页面。
11. 项目保存或 PDF 报告导出界面。

## 七、技术实现概要

- 开发语言：C++20
- GUI 框架：Qt 6 Widgets
- 本地数据库：SQLite
- Excel 读取：QXlsx
- PDF 读取：Qt PDF
- PDF 报告：QPdfWriter / QTextDocument
- 运行方式：Windows 原生桌面应用
- 项目文件：`.clrproj`
- 数据输入：`.csv`、`.xlsx`、内置数据集
- 资料输入：`.pdf`、`.txt`、`.md`、`.csv`、`.tsv`

## 八、建议登记名称

**软件全称：催化剂寿命数据分析与实验辅助系统**  
**建议简称：催化剂寿命分析系统**

该名称采用“应用对象 + 核心功能”的表述方式，能够覆盖数据检查、寿命分析、条件检查、实验建议、资料整理、项目保存和报告输出等已实现功能，同时避免将尚未完全接入的外部 AI 服务作为软件主体能力。